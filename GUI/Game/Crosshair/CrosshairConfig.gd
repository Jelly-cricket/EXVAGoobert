extends Resource
class_name CrosshairConfig 

@export var PrimaryColor = Color.WHITE ## Probably the actual thing.
@export var SecondaryColor = Color.BLACK ## Probably the outline.

func ExecuteDraw(controlParent : Control) -> void:
	pass
